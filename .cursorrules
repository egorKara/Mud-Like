# 🎯 Mud-Like Project Rules for Cursor AI Agents

## 📋 **ОБЩИЕ ПРИНЦИПЫ**

### **Архитектура проекта**
- **ECS-архитектура** - основа всего проекта, НИКАКИХ MonoBehaviour
- **Детерминизм** - критически важен для мультиплеера
- **Clean Architecture** - разделение по слоям (Domain, Application, Infrastructure)
- **SOLID принципы** - следование принципам объектно-ориентированного программирования

### **Технологический стек**
- **Unity 2022.3.62f1** - стабильная LTS версия Unity
- **DOTS (Data-Oriented Technology Stack)** - Entities, Components, Systems, Jobs
- **Unity Physics (DOTS)** - для реалистичной физики
- **Netcode for Entities** - для мультиплеера
- **URP (Universal Render Pipeline)** - для рендеринга

## 🏗️ **ПРАВИЛА РАЗРАБОТКИ**

### **1. ECS Компоненты**
```csharp
// ✅ ПРАВИЛЬНО - ECS компонент
public struct Position : IComponentData
{
    public float3 Value;
}

// ❌ НЕПРАВИЛЬНО - MonoBehaviour
public class PlayerController : MonoBehaviour
{
    // НЕ ИСПОЛЬЗОВАТЬ!
}
```

### **2. ECS Системы**
```csharp
// ✅ ПРАВИЛЬНО - ECS система
[UpdateInGroup(typeof(FixedStepSimulationSystemGroup))]
public partial class MovementSystem : SystemBase
{
    protected override void OnUpdate()
    {
        // Логика системы
    }
}
```

### **3. Детерминизм**
```csharp
// ✅ ПРАВИЛЬНО - детерминированный код
float deltaTime = SystemAPI.Time.fixedDeltaTime;
position.Value += velocity.Value * deltaTime;

// ❌ НЕПРАВИЛЬНО - недетерминированный код
float deltaTime = Time.deltaTime;
```

### **4. Производительность**
```csharp
// ✅ ПРАВИЛЬНО - использование Burst
[BurstCompile]
public struct OptimizedJob : IJobParallelFor
{
    public void Execute(int index)
    {
        // Высокопроизводительный код
    }
}
```

## 📁 **СТРУКТУРА ПРОЕКТА**

### **Модули и их назначение**
- **MudLike.Core** - базовые компоненты и системы
- **MudLike.Vehicles** - транспорт и физика
- **MudLike.Terrain** - деформация террейна
- **MudLike.Networking** - мультиплеер
- **MudLike.DOTS** - центральные DOTS системы
- **MudLike.Tests** - тестирование

### **Правила размещения файлов**
- **Components/** - ECS компоненты данных
- **Systems/** - ECS системы
- **Configs/** - ScriptableObject конфигурации
- **Data/** - структуры данных

## 🧪 **ТЕСТИРОВАНИЕ**

### **Обязательные тесты**
- **Unit тесты** для всех публичных методов
- **Integration тесты** для взаимодействия систем
- **Performance тесты** для критических путей
- **Code Coverage >80%** для всех модулей

### **Структура тестов**
```csharp
[Test]
public void MethodName_ValidInput_ReturnsExpectedResult()
{
    // Arrange
    var input = CreateTestInput();
    
    // Act
    var result = MethodUnderTest(input);
    
    // Assert
    Assert.AreEqual(expected, result);
}
```

## 📝 **СТАНДАРТЫ КОДИРОВАНИЯ**

### **Именование**
- **Классы/Структуры** - PascalCase
- **Методы** - PascalCase
- **Поля** - camelCase с подчеркиванием (_fieldName)
- **Константы** - UPPER_CASE

### **Документация**
```csharp
/// <summary>
/// Краткое описание компонента/системы
/// </summary>
/// <remarks>
/// Подробное описание функциональности
/// </remarks>
public struct ComponentName : IComponentData
{
    /// <summary>
    /// Описание поля
    /// </summary>
    public float3 Value;
}
```

### **Комментарии в коде**
```csharp
// ✅ ПРАВИЛЬНО - объяснение сложной логики
// Вычисляем деформацию террейна на основе давления колеса
var deformation = CalculateDeformation(wheelPressure, terrainHardness);

// ❌ НЕПРАВИЛЬНО - очевидные комментарии
// Увеличиваем позицию
position += velocity * deltaTime;
```

## 🚫 **ЗАПРЕЩЕНО**

### **1. MonoBehaviour**
- НЕ ИСПОЛЬЗОВАТЬ MonoBehaviour в игровом коде
- НЕ ИСПОЛЬЗОВАТЬ GameObject для игровых объектов
- НЕ ИСПОЛЬЗОВАТЬ Transform для позиций

### **2. Недетерминированный код**
- НЕ ИСПОЛЬЗОВАТЬ Time.deltaTime в ECS системах
- НЕ ИСПОЛЬЗОВАТЬ Random.Range в детерминированном коде
- НЕ ИСПОЛЬЗОВАТЬ недетерминированные алгоритмы

### **3. Плохая производительность**
- НЕ ИСПОЛЬЗОВАТЬ foreach в критических путях
- НЕ ИСПОЛЬЗОВАТЬ LINQ в ECS системах
- НЕ ИСПОЛЬЗОВАТЬ string concatenation в циклах

## ✅ **ОБЯЗАТЕЛЬНО**

### **1. ECS архитектура**
- ВСЕГДА использовать ECS компоненты
- ВСЕГДА использовать ECS системы
- ВСЕГДА использовать Job System для параллельных операций

### **2. Детерминизм**
- ВСЕГДА использовать Time.fixedDeltaTime
- ВСЕГДА использовать FixedStepSimulationSystemGroup
- ВСЕГДА тестировать детерминизм

### **3. Производительность**
- ВСЕГДА использовать Burst Compiler где возможно
- ВСЕГДА использовать NativeArray для больших данных
- ВСЕГДА профилировать критический код

### **4. Тестирование**
- ВСЕГДА писать тесты для нового кода
- ВСЕГДА проверять Code Coverage
- ВСЕГДА тестировать производительность

## 🎯 **ПРИОРИТЕТЫ РАЗРАБОТКИ**

### **Высокий приоритет**
1. **ECS миграция** - основа всего проекта
2. **Детерминизм** - критично для мультиплеера
3. **Производительность** - заложена в архитектуру
4. **Тестирование** - качество с первого дня

### **Средний приоритет**
1. **Деформация террейна** - сложная, но важная задача
2. **UI/UX** - пользовательский опыт
3. **Аудио** - погружение в игру

### **Низкий приоритет**
1. **Дополнительные эффекты** - визуальные улучшения
2. **Дополнительный контент** - карты, транспорт

## 📊 **МЕТРИКИ КАЧЕСТВА**

### **Технические метрики**
- **Code Coverage:** >80%
- **Performance:** 60+ FPS на целевой аппаратуре
- **Network:** <100ms задержка между клиентами
- **Stability:** <1% отключений в сессии

### **Качественные метрики**
- **Детерминированная симуляция** на всех клиентах
- **Реалистичная деформация** террейна
- **Высокая производительность** (60+ FPS)
- **Качественный код** с тестами

## 🔧 **ИНСТРУМЕНТЫ**

### **Обязательные инструменты**
- **Unity Profiler** - анализ производительности
- **Code Coverage** - измерение покрытия тестами
- **Unity Test Runner** - запуск тестов
- **Burst Inspector** - анализ Burst кода

### **Рекомендуемые инструменты**
- **Unity Memory Profiler** - анализ памяти
- **Unity Frame Debugger** - отладка рендеринга
- **Unity Network Profiler** - анализ сетевого трафика

## 🎮 **ИГРОВЫЕ СИСТЕМЫ**

### **Физика транспорта**
- **ECS компоненты колес** для детерминированной физики
- **Unity Physics (DOTS)** для коллизий
- **Система подвески** с реалистичными параметрами

### **Деформация террейна**
- **MudManager API** для взаимодействия с грязью
- **Heightfield per block** для деформации
- **Синхронизация TerrainData + TerrainCollider**

### **Мультиплеер**
- **Netcode for Entities** для синхронизации
- **Client-Server архитектура** с авторитарным сервером
- **Lag Compensation** для компенсации задержек

## 📚 **ДОКУМЕНТАЦИЯ**

### **Обязательная документация**
- **XML комментарии** для всех публичных API
- **README файлы** для каждого модуля
- **Примеры использования** для сложных систем
- **Архитектурные диаграммы** для сложных взаимодействий

### **Рекомендуемая документация**
- **Troubleshooting** для типичных проблем
- **Performance guides** для оптимизации
- **Migration guides** для обновлений

## 🔍 **АВТОМАТИЧЕСКАЯ ПРОВЕРКА ИМЕНОВАНИЯ**

### **Bash скрипты для работы с именованием**
```bash
# Запуск проверки именования
./Scripts/naming_check.sh

# Или с подробным выводом
bash -x ./Scripts/naming_check.sh

# Автоматическое исправление нарушений
./Scripts/fix_naming.sh
```

### **Что проверяет скрипт**
- **ECS системы** - правильные суффиксы и отсутствие конфликтов
- **ECS компоненты** - запрещенные имена
- **Менеджеры и пулы** - правильные суффиксы
- **Jobs** - правильные суффиксы и имена
- **Namespace** - соответствие структуре MudLike.Module.Submodule
- **Assembly Definitions** - наличие в модульных папках
- **Unity API конфликты** - запрещенные Unity классы

### **Интеграция с Cursor IDE**
```bash
# Добавить в .git/hooks/pre-commit
#!/bin/bash
./Scripts/naming_check.sh
```

### **Автоматическая проверка при коммите**
```bash
# Создать pre-commit hook
echo '#!/bin/bash' > .git/hooks/pre-commit
echo './Scripts/naming_check.sh' >> .git/hooks/pre-commit
chmod +x .git/hooks/pre-commit
```

---
"Always wrap classes in namespaces like MudLike.Module.Submodule". Prompt: "Refactor all ECS classes to use namespaces" (agent mode).

"When generating ECS, use ISystem with BurstCompile".

"Use memories for ECS patterns".

"Generate and run tests on code change" — для 100% coverage.

**Эти правила обеспечивают единообразие, качество и производительность кода в проекте Mud-Like. Следование этим правилам критически важно для успеха проекта.**
